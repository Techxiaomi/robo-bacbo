# Segredos e ambiente local
.env
.env.*
!.env.example

# Sessões autenticadas / cookies / storage state
sessao_salva.json
**/sessao_salva.json
*.storage-state.json
*.cookies.json

# Dependências
node_modules/
venv/
.venv/
__pycache__/
*.py[cod]

# Logs, caches e arquivos temporários
*.log
logs/
robo-sync-pilot/runtime/
robo-bacbo/public/bacbo-map-snapshot.json
.cache/
.pytest_cache/
coverage/
.nyc_output/

# Artefatos locais de operação / restauração
# Não fazem parte do código-fonte autoritativo do repositório.
/BR/
/INT/
/backup/
/garnet/
/runtime/
/install.json

# IMPORTANTE: /tools/ fica propositalmente visível até ser classificado.
# Pode conter utilitários que devam ser versionados; não ignorar às cegas.

# IDE / SO
.vscode/
.idea/
.DS_Store
Thumbs.db

# Chaves e certificados
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.json
