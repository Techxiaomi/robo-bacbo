name: BUG-043 modal cleanup patch

on:
  push:
    branches:
      - agent/bug-023-websocket-reconnect-continuity
    paths:
      - .github/workflows/bug043-modal-cleanup.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: agent/bug-023-websocket-reconnect-continuity
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply BUG-043 patch
        run: python scripts/apply_bug043_modal_cleanup_quick.py
      - name: Syntax checks
        run: |
          python -m py_compile robo-sync-pilot/robo.py robo-sync-pilot/tests/test_bug038_contract.py
      - name: Commit patched files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add robo-sync-pilot/robo.py robo-sync-pilot/tests/test_bug038_contract.py
          if git diff --cached --quiet; then
            echo "No BUG-043 changes to commit"
            exit 0
          fi
          git commit -m "fix: close blocking modal before financial wait"
          git push origin HEAD:agent/bug-023-websocket-reconnect-continuity
