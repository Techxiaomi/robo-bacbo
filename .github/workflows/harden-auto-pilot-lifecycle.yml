name: Harden Auto Pilot Lifecycle

on:
  push:
    branches:
      - agent/auto-pilot-ia-selection-engine
    paths:
      - .github/scripts/harden_auto_pilot_lifecycle.py
      - .github/workflows/harden-auto-pilot-lifecycle.yml
      - robo-bacbo/test/auto_pilot_lifecycle.test.js

permissions:
  contents: write

jobs:
  apply-and-validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/auto-pilot-ia-selection-engine
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Apply lifecycle hardening
        run: python .github/scripts/harden_auto_pilot_lifecycle.py

      - name: Syntax checks
        run: |
          node --check robo-bacbo/auto_pilot_ia.js
          node --check robo-bacbo/bot2_coletor.js

      - name: Full Node regression suite
        working-directory: robo-bacbo
        run: npm test

      - name: Diff check
        run: git diff --check

      - name: Remove temporary applicator
        run: |
          rm .github/scripts/harden_auto_pilot_lifecycle.py
          rm .github/workflows/harden-auto-pilot-lifecycle.yml

      - name: Commit validated hardening
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add robo-bacbo/auto_pilot_ia.js robo-bacbo/bot2_coletor.js robo-bacbo/test/auto_pilot_lifecycle.test.js docs/AUTO_PILOT_IA.md .github/scripts/harden_auto_pilot_lifecycle.py .github/workflows/harden-auto-pilot-lifecycle.yml
          git diff --cached --check
          git commit -m "AUTO-PILOT: endurecer ciclo de vida dos padrões IA"
          git push origin HEAD:agent/auto-pilot-ia-selection-engine
